# Placeholder secrets for local/dev clusters ONLY. In production these
# values must be provisioned by the dedicated Secrets Management Layer
# (Vault/KMS — see docs/architecture.md §1) and never committed.
apiVersion: v1
kind: Secret
metadata:
  name: git-viz-secrets
type: Opaque
stringData:
  jwt-secret: "super-secret-system-key-rotate-in-production"
  github-client-id: ""
  github-client-secret: ""

{
  "name": "@git-viz/utils",
  "version": "1.0.0",
  "private": true,
  "description": "Shared helper functions: prefixed-hash codec for the <RepoID>_<SHA> node id scheme and viewport math used by the canvas.",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "^5.4.5",
    "vitest": "^4.1.8"
  },
  "dependencies": {
    "vite": "8.0.16"
  }
}
